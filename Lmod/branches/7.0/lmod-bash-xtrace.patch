--- Lmod-6.6/init/bash.in	2016-12-15 17:25:30.645403685 +0100
+++ Lmod-6.6-new/init/bash.in	2016-12-15 17:12:17.217391931 +0100
@@ -1,4 +1,7 @@
 # -*- shell-script -*-
+xtrace_saved="$(set -o | grep xtrace | grep 'on')";
+set +x
+
 if [[ ${-/x} != $- ]]; then
    echo "Start of Lmod init/bash script to define the module command"
 fi
@@ -137,6 +140,9 @@
    echo "End of Lmod init/bash script to define the module command"
 fi
 
+[ -n "$xtrace_saved" ] && set -x 
+unset xtrace_saved
+
 # Local Variables:
 # mode: shell-script
 # indent-tabs-mode: nil
